# Payload
PAYLOAD_SECRET=your-secret-key-change-this

# Database (SQLite for dev — zero config)
# For production Postgres, swap adapter in payload.config.ts and use:
# DATABASE_URI=postgresql://user:pass@localhost:5432/dharmony

# Site
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# WP Migration (only needed for migrate-from-wp script)
# WP_API_URL=http://your-old-wp-site/wp-json
